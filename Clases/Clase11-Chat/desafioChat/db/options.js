const options = {
    client: 'mysql',
    connection : {
        host : '127.0.0.1',
        root : 'root',
        password : 'your_password',
        database : 'mensajes'
    }
}

module.exports = { options }